import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sponsors",
  description: "Title sponsors, partners and confirmed budgets.",
};

export default function DashboardDashboardSponsorsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
