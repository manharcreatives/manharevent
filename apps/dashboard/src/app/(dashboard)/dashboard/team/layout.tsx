import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Team",
  description: "Who can do what, and who can open the gate scanner.",
};

export default function DashboardDashboardTeamLayout({ children }: { children: React.ReactNode }) {
  return children;
}
