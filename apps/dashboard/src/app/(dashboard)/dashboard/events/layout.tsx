import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Events",
  description: "Every season you run, with sales and capacity at a glance.",
};

export default function DashboardDashboardEventsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
