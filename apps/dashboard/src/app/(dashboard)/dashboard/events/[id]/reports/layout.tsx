import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Reports",
  description: "Sales by zone, night and pass type.",
};

export default function DashboardDashboardEventsIdReportsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
