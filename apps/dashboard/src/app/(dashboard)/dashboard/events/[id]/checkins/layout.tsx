import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Check-ins",
  description: "Every scan at every gate, allowed and denied.",
};

export default function DashboardDashboardEventsIdCheckinsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
