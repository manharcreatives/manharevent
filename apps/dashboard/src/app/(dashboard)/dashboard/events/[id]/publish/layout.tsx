import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Publish",
  description: "Pre-publish checks and the share link for your booking page.",
};

export default function DashboardDashboardEventsIdPublishLayout({ children }: { children: React.ReactNode }) {
  return children;
}
