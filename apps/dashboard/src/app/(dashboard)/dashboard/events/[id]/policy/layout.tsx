import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Refund & re-entry policy",
  description: "What buyers are promised, and what the gates enforce.",
};

export default function DashboardDashboardEventsIdPolicyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
