import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Promo codes",
  description: "Discount codes buyers enter at checkout.",
};

export default function DashboardDashboardEventsIdPromosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
