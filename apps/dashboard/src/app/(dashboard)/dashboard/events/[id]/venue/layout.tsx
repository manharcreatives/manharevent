import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Venue & zones",
  description: "Ground layout, zone capacities and gates.",
};

export default function DashboardDashboardEventsIdVenueLayout({ children }: { children: React.ReactNode }) {
  return children;
}
