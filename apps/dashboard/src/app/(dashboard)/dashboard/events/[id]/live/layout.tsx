import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Live ops",
  description: "Gate-by-gate occupancy while the ground is open.",
};

export default function DashboardDashboardEventsIdLiveLayout({ children }: { children: React.ReactNode }) {
  return children;
}
