import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add-ons",
  description: "Parking, dinner coupons and merchandise sold with a pass.",
};

export default function DashboardDashboardEventsIdAddonsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
