import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pass types",
  description: "Inventory, admits, nights and price tiers.",
};

export default function DashboardDashboardEventsIdPassesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
