import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Attendees",
  description: "Search every buyer, pass and order for this season.",
};

export default function DashboardDashboardEventsIdAttendeesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
