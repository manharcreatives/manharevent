import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Comp passes",
  description: "Complimentary passes for guests, media and sponsors.",
};

export default function DashboardDashboardEventsIdCompsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
