import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Nights",
  description: "Themes, lineup, dress codes and gate timings per night.",
};

export default function DashboardDashboardEventsIdNightsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
