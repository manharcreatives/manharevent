import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Branding",
  description: "Your colours and logo on the public booking page.",
};

export default function DashboardDashboardSettingsBrandingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
