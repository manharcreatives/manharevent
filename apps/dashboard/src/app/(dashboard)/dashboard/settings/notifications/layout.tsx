import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Notifications",
  description: "Which WhatsApp, SMS and email templates send.",
};

export default function DashboardDashboardSettingsNotificationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
