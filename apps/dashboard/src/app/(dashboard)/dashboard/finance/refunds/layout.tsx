import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Refund queue",
  description: "Requests waiting on your approval.",
};

export default function DashboardDashboardFinanceRefundsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
