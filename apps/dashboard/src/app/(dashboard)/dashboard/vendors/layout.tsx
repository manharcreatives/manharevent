import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Vendors",
  description: "Food stalls, merchandise and parking operators.",
};

export default function DashboardDashboardVendorsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
